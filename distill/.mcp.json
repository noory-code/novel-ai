{
  "mcpServers": {
    "distill": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "--directory",
        "${CLAUDE_PLUGIN_ROOT}",
        "run",
        "python",
        "-m",
        "distill"
      ]
    }
  }
}
