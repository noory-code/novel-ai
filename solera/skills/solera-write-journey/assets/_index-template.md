# Journey Map

> The living catalog of how Personas move through this service.
> Each entry is a link to an individual Journey file in `journeys/`, grouped by the Persona that walks it.

## Active

<!-- Auto-maintained by solera-write-journey at Wrap-up. Active Journeys are grouped by
     the Persona that walks them, then indented by `parent` chain within each group.
     Top-level Journeys (no `parent:`) sit at column 0 within their group; children
     indent two spaces per depth, keeping the file format grep-friendly. -->

(no active Journeys yet)

## Deprecated

<!-- Journeys with `status: deprecated`. Kept visible because Narratives may still anchor to them. -->

(none)

## Archived

<!-- Archived Journeys are not listed here. They remain on disk at `journeys/{id}.md` with `status: archived`. -->
