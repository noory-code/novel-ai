# Persona Map

> The living catalog of who this service is for. Humans draw Personas; AI proposes and questions.
> Each entry is a link to an individual Persona file in `personas/`.

## Active

<!-- Auto-maintained by solera-write-persona at Wrap-up. Every file with `status: active`
     in its frontmatter appears here, indented by parent chain so the audience hierarchy
     is visible at a glance. Top-level Personas (no `parent:`) sit at column 0; children
     indent two spaces per depth, keeping the file format grep-friendly. -->

(no active Personas yet)

## Deprecated

<!-- Personas with `status: deprecated`. Kept visible because Narratives may still reference them. -->

(none)

## Archived

<!-- Archived Personas are not listed here. They remain on disk at `personas/{id}.md` with `status: archived`. -->
