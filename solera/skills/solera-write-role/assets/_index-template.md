# Role Map

> The living catalog of structural user classes this service is for.
> Humans draw Roles; AI proposes and questions. Individual archetypes
> (Personas) hang off a specific Role when a vertical deepens.
> Each entry is a link to an individual Role file in `roles/`.

## Active

<!-- Auto-maintained by solera-write-role at Wrap-up. Every file with
     `status: active` in its frontmatter appears here, indented by
     parent chain so the Role hierarchy is visible at a glance.
     Top-level Roles (no `parent:`) sit at column 0; children indent
     two spaces per depth, keeping the file format grep-friendly. -->

(no active Roles yet)

## Deprecated

<!-- Roles with `status: deprecated`. Kept visible because Personas /
     Journeys / Narratives may still reference them. -->

(none)

## Archived

<!-- Archived Roles are not listed here. They remain on disk at
     `roles/{id}.md` with `status: archived`. -->
