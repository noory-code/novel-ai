"""Solera — a slim harness over a plain-file ``.noory/solera/`` workspace.

Solera plans work into Stories and Actions, runs deterministic gates, and orders
the steps an external AI agent executes. It does not run the agent loop itself and
it works standalone, with or without Plot.
"""

__version__ = "6.0.0"
