node_modules/
*.log
.env
.env.local
.DS_Store

# The root repo .gitignore excludes `dist/` (for Python packaging). For the
# viewer, dist/ is the built static bundle that must ship with the plugin.
!dist/
!dist/**
